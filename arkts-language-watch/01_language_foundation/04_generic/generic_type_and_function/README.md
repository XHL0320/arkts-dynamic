# generic type and function

## 目录用途
当前叶子目录用于看护单个 ArkTS 语言特性测试点，沉淀该特性的规范、设计和验证资产。

## 后续沉淀内容
后续应补充 `spec_summary.md`、`test_design.md`、`coverage_matrix.md`，并在 `xts/` 下维护正向、编译负向、运行时失败和回归用例。

## 看护关系
该目录是 ArkTS 语言看护的最小资产单元，用于把官方规则映射到 XTS 用例、编译负向检查、覆盖矩阵和问题回归记录。
