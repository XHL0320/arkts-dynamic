# 类装饰器返回值边界

## 边界点

class decorator 返回 `void` 是普通 smoke 场景。返回原 class、返回新 class、返回非 class value 的行为需要结合 ArkTS checker、runtime 和 lowering 规则确认。

## 建议

- 返回 `void`：可转 pass/smoke。
- 返回新 class：建议作为 boundary / compiler expected candidate。
- 返回非 class：建议作为 fail_compile 或 fail_runtime 待确认。