# 属性类型错误负向示例

## 错误示例

```ts
function dec(target: Object, key: string): void {}
class Demo {
  @dec
  value: number = "wrong"
}
```

## 预期

编译失败。

## 原因

属性声明为 `number`，初始化值为 `string`。decorator 不应绕过字段类型检查。

## 测试设计建议

适合转为 fail_compile 用例，不适合放入 ohosTest 正向执行。